<% @favourites.each do |favourite|%>

  <div class="card-products">

    <img src= <%=favourite.car.image_url %>/>
    <div class="card-products-infos">
      <h1>My Garage</h1>


      <h2><%= favourite.car.brand %></h2>
      <h2><%= favourite.car.model %></h2>
      <p><%= favourite.car.year %> | <%= favourite.car.fuel %> | <%= link_to "🗑️", favourite_path(favourite), data: {turbo_confirm: "are you sure?"} %></p>



    </div>
  </div>
 <%end%>
